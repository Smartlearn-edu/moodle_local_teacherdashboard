<div class="container-fluid dashboard-container">
    <div class="row mb-4">
        <div class="col-12">
            <h2 class="dashboard-title">{{#str}}pluginname, local_teacherdashboard{{/str}}</h2>
            <p class="text-muted">Welcome back! Here are the courses you are teaching.</p>
        </div>
    </div>

    <div class="row course-grid">
        {{#courses}}
        <div class="col-md-4 col-sm-6 mb-4">
            <div class="card h-100 shadow-sm border-0 course-card">
                <div class="card-img-top-wrapper" style="height: 160px; overflow: hidden; background-color: #f5f5f5; position: relative;">
                    {{#imageurl}}
                    <img src="{{imageurl}}" class="card-img-top h-100 w-100 object-fit-cover" alt="{{fullname}}">
                    {{/imageurl}}
                    {{^imageurl}}
                    <div class="d-flex align-items-center justify-content-center h-100 text-secondary">
                        <i class="fa fa-graduation-cap fa-3x"></i>
                    </div>
                    {{/imageurl}}
                    
                    <div class="course-category-badge position-absolute top-0 end-0 m-2 badge bg-primary bg-opacity-75">
                        {{categoryname}}
                    </div>
                </div>
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title text-truncate" title="{{fullname}}">
                        <a href="{{viewurl}}" class="text-decoration-none text-dark stretched-link">{{fullname}}</a>
                    </h5>
                    <div class="mt-auto pt-3 d-flex justify-content-between align-items-center">
                        <span class="text-muted small">
                            <i class="fa fa-users me-1"></i> {{studentcount}} {{#str}}students, local_teacherdashboard{{/str}}
                        </span>
                        {{#hasgrading}}
                        <span class="badge bg-danger rounded-pill" title="{{gradingcount}} {{#str}}needsgrading, local_teacherdashboard{{/str}}">
                            <i class="fa fa-check-square-o me-1"></i> {{gradingcount}}
                        </span>
                        {{/hasgrading}}
                    </div>
                </div>
            </div>
        </div>
        {{/courses}}
        {{^courses}}
        <div class="col-12">
            <div class="alert alert-info" role="alert">
                {{#str}}nocourses, local_teacherdashboard{{/str}}
            </div>
        </div>
        {{/courses}}
    </div>
</div>

<style>
.dashboard-title {
    font-weight: 700;
    color: #333;
}
.course-card {
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.course-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
}
.object-fit-cover {
    object-fit: cover;
}
</style>
